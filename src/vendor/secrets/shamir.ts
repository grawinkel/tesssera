/**
 * Vendored from: shamir-secret-sharing v0.0.4 by Privy (Apache-2.0)
 * Source: https://github.com/privy-io/shamir-secret-sharing
 * Audited by: Cure53, Zellic
 *
 * Shamir's Secret Sharing over GF(2^8).
 * Information-theoretically secure — quantum-resistant by nature.
 */

// CSPRNG: uses browser's crypto.getRandomValues
// crypto.getRandomValues caps at 65536 bytes per call, so we chunk large requests.
function getRandomBytes(numBytes: number): Uint8Array {
  const buf = new Uint8Array(numBytes);
  const MAX_CHUNK = 65536;
  for (let offset = 0; offset < numBytes; offset += MAX_CHUNK) {
    const end = Math.min(offset + MAX_CHUNK, numBytes);
    crypto.getRandomValues(buf.subarray(offset, end));
  }
  return buf;
}

// The Polynomial used is: x^8 + x^4 + x^3 + x + 1
//
// Lookup tables pulled from:
//   https://github.com/hashicorp/vault/blob/9d46671659cbfe7bbd3e78d1073dfb22936a4437/shamir/tables.go
//   http://www.samiam.org/galois.html
//
// 0xe5 (229) is used as the generator.

// Provides log(X)/log(g) at each index X.
const LOG_TABLE: Readonly<Uint8Array> = new Uint8Array([
  ,
]);

// Provides the exponentiation value at each index X.
const EXP_TABLE: Readonly<Uint8Array> = new Uint8Array([
  ,
]);

function add(a: number, b: number): number {
  return a ^ b;
}

function div(a: number, b: number): number {
  if (b === 0) {
    throw new Error('cannot divide by zero');
  }
  const logA = LOG_TABLE[a]!;
  const logB = LOG_TABLE[b]!;
  const diff = (logA - logB + 255) % 255;
  const result = EXP_TABLE[diff]!;
  return a === 0 ? 0 : result;
}

function mult(a: number, b: number): number {
  const logA = LOG_TABLE[a]!;
  const logB = LOG_TABLE[b]!;
  const sum = (logA + logB) % 255;
  const result = EXP_TABLE[sum]!;
  return a === 0 || b === 0 ? 0 : result;
}

function interpolatePolynomial(xSamples: Uint8Array, ySamples: Uint8Array, x: number): number {
  const limit = xSamples.length;
  let basis = 0;
  let result = 0;

  for (let i = 0; i < limit; i++) {
    basis = 1;
    for (let j = 0; j < limit; ++j) {
      if (i === j) continue;
      const num = add(x, xSamples[j]!);
      const denom = add(xSamples[i]!, xSamples[j]!);
      const term = div(num, denom);
      basis = mult(basis, term);
    }
    result = add(result, mult(ySamples[i]!, basis));
  }

  return result;
}

function evaluate(coefficients: Uint8Array, x: number, degree: number) {
  if (x === 0) {
    throw new Error('cannot evaluate secret polynomial at zero');
  }
  let result = coefficients[degree]!;
  for (let i = degree - 1; i >= 0; i--) {
    const coefficient = coefficients[i]!;
    result = add(mult(result, x), coefficient);
  }
  return result;
}

function newCoordinates(): Readonly<Uint8Array> {
  const coordinates = new Uint8Array(255);
  for (let i = 0; i < 255; i++) {
    coordinates[i] = i + 1;
  }
  // Biased shuffle — acceptable for SSS (security does not depend on coordinate ordering)
  const randomIndices = getRandomBytes(255);
  for (let i = 0; i < 255; i++) {
    const j = randomIndices[i]! % 255;
    const temp = coordinates[i]!;
    coordinates[i] = coordinates[j]!;
    coordinates[j] = temp;
  }
  return coordinates;
}

export async function split(
  secret: Uint8Array,
  shares: number,
  threshold: number,
): Promise<Uint8Array[]> {
  if (!(secret instanceof Uint8Array)) throw new TypeError('secret must be a Uint8Array');
  if (secret.byteLength < 1) throw new Error('secret cannot be empty');
  if (shares < 2 || shares > 255) throw new RangeError('shares must be between 2 and 255');
  if (threshold < 2 || threshold > 255) throw new RangeError('threshold must be between 2 and 255');
  if (shares < threshold) throw new Error('shares cannot be less than threshold');

  const result: Uint8Array[] = [];
  const secretLength = secret.byteLength;
  const xCoordinates = newCoordinates();

  for (let i = 0; i < shares; i++) {
    const share = new Uint8Array(secretLength + 1);
    share[secretLength] = xCoordinates[i]!;
    result.push(share);
  }

  const degree = threshold - 1;

  // Pre-generate all random coefficients in one batch instead of per-byte.
  // Each byte of the secret needs `degree` random coefficients.
  const allRandom = getRandomBytes(secretLength * degree);
  const coefficients = new Uint8Array(degree + 1);

  for (let i = 0; i < secretLength; i++) {
    // Build coefficients inline: intercept = secret byte, rest from pre-generated random
    coefficients[0] = secret[i]!;
    const randomOffset = i * degree;
    for (let d = 0; d < degree; d++) {
      coefficients[d + 1] = allRandom[randomOffset + d]!;
    }
    for (let j = 0; j < shares; ++j) {
      const x = xCoordinates[j]!;
      const y = evaluate(coefficients, x, degree);
      result[j]![i] = y;
    }
  }

  return result;
}

export async function combine(shares: Uint8Array[]): Promise<Uint8Array> {
  if (!Array.isArray(shares)) throw new TypeError('shares must be an Array');
  if (shares.length < 2 || shares.length > 255) {
    throw new RangeError('shares must have between 2 and 255 elements');
  }

  const share1 = shares[0]!;
  for (const share of shares) {
    if (!(share instanceof Uint8Array)) throw new TypeError('each share must be a Uint8Array');
    if (share.byteLength < 2) throw new Error('each share must be at least 2 bytes');
    if (share.byteLength !== share1.byteLength) {
      throw new Error('all shares must have the same byte length');
    }
  }

  const sharesLength = shares.length;
  const shareLength = share1.byteLength;
  const secretLength = shareLength - 1;
  const secret = new Uint8Array(secretLength);

  const xSamples = new Uint8Array(sharesLength);
  const ySamples = new Uint8Array(sharesLength);

  const samples: Set<number> = new Set();
  for (let i = 0; i < sharesLength; i++) {
    const share = shares[i]!;
    const sample = share[shareLength - 1]!;
    if (samples.has(sample)) {
      throw new Error('shares must contain unique values but a duplicate was found');
    }
    samples.add(sample);
    xSamples[i] = sample;
  }

  for (let i = 0; i < secretLength; i++) {
    for (let j = 0; j < sharesLength; ++j) {
      ySamples[j] = shares[j]![i]!;
    }
    secret[i] = interpolatePolynomial(xSamples, ySamples, 0);
  }

  return secret;
}
